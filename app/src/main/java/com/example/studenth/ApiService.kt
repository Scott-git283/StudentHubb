package com.example.studenth

import com.example.studenth.model.*
import retrofit2.Call
import retrofit2.http.*

// This interface is now almost empty. Only methods that have not been migrated to Firebase remain.
interface ApiService {
    // All methods related to courses, departments, and faculty have been removed.
}
